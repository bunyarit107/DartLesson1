void main(List<String> args) {
  String title = 'Dart course';
  print(title);
  // ตัวพิมใหญ่
  title = title.toUpperCase();
  print(title);
  // ตัวพิมเล็ก
  title = title.toLowerCase();
  print(title);

}